rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Development mode: Allow all read/write for the custom manual auth flow
    // since Owner login doesn't use Firebase Auth, it must be able to query the DB directly.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
